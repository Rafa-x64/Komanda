---
trigger: always_on
---

Los archivos que debe llevar si o si Komanda-api son:

module.validator.ts
module.route.ts
module.controller.ts
module.service.ts

opcional (domain/DBTable.entity.ts) si no existe o no esta creada ya la entidad... de lo contrario usar la que ya esta hecha